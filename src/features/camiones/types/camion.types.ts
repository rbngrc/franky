export type Camion = {
  matricula: string;
  marca: string;
  modelo: string;
  capacidadToneladas: number;
  tara: number;
  activo: boolean;
};

export type CamionFormData = {
  matricula: string;
  marca: string;
  modelo: string;
  capacidadToneladas: number;
  tara: number;
};
